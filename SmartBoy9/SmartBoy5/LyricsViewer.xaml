<Window x:Class="SmartBoy.LyricsViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Path=Title}" Height="{Binding Path=LyricsLineCount}" Width="300" Background="{Binding Path=DominantColor}">
    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Auto">
            <TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="{Binding Path=Lyrics}" VerticalAlignment="Top" Width="150" Foreground="{Binding Path=ContrastColor}" FontSize="12"/>
        </ScrollViewer>
    </Grid>
</Window>
